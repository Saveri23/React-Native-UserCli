# User Directory App

A simple React Native app to display users fetched from a public API, with search, pull-to-refresh, and dark/light theme support.

## Features
- Display list of users fetched from API
- Search users by name
- Pull-to-refresh to reload users
- Dark/Light theme toggle (☀️ / 🌙)
- Redux state management
- Persist data with AsyncStorage
- Smooth performance on mobile devices

## Screens
- **HomeScreen**: Search bar + user list (clickable cards)
- Dark/Light theme changes entire screen background
- Pull-to-refresh reloads users

## Installation
```bash

# Start Expo server
npm start
